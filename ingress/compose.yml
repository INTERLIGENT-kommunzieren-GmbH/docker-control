services:
  nginx:
    container_name: nginx-proxy
    image: nginxproxy/nginx-proxy:1.6
    networks:
      - "frontend-tier"
    ports:
      - "80:80"
      - "443:443"
    restart: always
    environment:
      DOCKER_HOST: tcp://localhost:2375
    volumes:
      - "${PLUGIN_MOUNTS_DIR}/ingress/vhosts.d:/etc/nginx/vhost.d:ro"
      - "${PLUGIN_MOUNTS_DIR}/ingress/tls:/etc/nginx/certs:ro"
    logging:
      options:
        max-size: "100M"
        max-file: "4"

  proxy-companion:
    container_name: proxy-companion
    image: sebastienheyd/self-signed-proxy-companion
    environment:
      NGINX_PROXY_CONTAINER: nginx-proxy
      DOCKER_HOST: tcp://localhost:2375
    networks:
      - "frontend-tier"
    restart: always
    volumes:
      - "${PLUGIN_MOUNTS_DIR}/ingress/tls:/etc/nginx/certs:rw"

networks:
  frontend-tier:
    name: proxy

